# NetLet configuration.
#
# `pnpm setup:env` writes the .env this is a template for, generating JWT_SECRET
# and — given the service-account JSON — the three Firebase values, whose
# escaping is the usual way this setup fails. Or copy it by hand: cp .env.example .env
#
# Nothing here throws at boot — the server starts on an empty file — so an unset
# variable disables a feature rather than preventing startup. The tiers below
# say which.
#
# VITE_-prefixed values are inlined into the client bundle at build time and are
# therefore PUBLIC. Never put a secret behind a VITE_ name.

# ---------------------------------------------------------------------------
# Tier 1 — the storefront. Without these there is no catalog: every product,
# collection and cart call fails with "Shopify Storefront API is not
# configured", which is most of the site.
# ---------------------------------------------------------------------------

# Your myshopify domain, no scheme. e.g. netlet.myshopify.com
SHOPIFY_STORE_DOMAIN=

# Storefront API access token — NOT an Admin API token. Shopify admin ->
# Settings -> Apps and sales channels -> Develop apps -> your app ->
# Configuration -> Storefront API. Needs at minimum:
#   unauthenticated_read_product_listings
#   unauthenticated_read_product_inventory
#   unauthenticated_write_checkouts   (cart create / update)
# The adapter pins Storefront API version 2025-04 (server/_core/shopify.ts).
SHOPIFY_STOREFRONT_API_ACCESS_TOKEN=

# ---------------------------------------------------------------------------
# Tier 2 — accounts. Without these the site still sells: browsing, cart and
# checkout all work as a guest. Sign-in, saved items, delivery preference and
# order tracking are what go dark.
#
# Registered accounts live in Firebase Authentication (Tier 2b below). Postgres
# still holds one mirror row per shopper, because saved products, delivery area
# and notification preferences are foreign keys to it. Leave the Firebase block
# empty and the same endpoints fall back to a local scrypt digest in that same
# table, so a deployment without a Firebase project still signs people in.
# ---------------------------------------------------------------------------

# Postgres connection string.
#
# Supabase: Project Settings -> Database -> Connection string -> URI. Take the
# DIRECT connection (port 5432), not the transaction pooler (6543) — this runs
# as a long-lived server, not serverless functions. The pooler also works: the
# client sets `prepare: false` so prepared statements are never assumed.
#
#   postgresql://postgres:PASSWORD@db.PROJECT.supabase.co:5432/postgres
#
# Provision the five tables with: pnpm db:push
# Inspect them afterwards with:   pnpm db:studio
DATABASE_URL=

# Signs session cookies (HS256, via jose). Required before any sign-in works.
# Generate with:  openssl rand -base64 48
# Rotating it invalidates every existing session, which is the intended way to
# sign everyone out.
JWT_SECRET=

# Address granted the admin role when it registers. Optional.
OWNER_EMAIL=

# ---------------------------------------------------------------------------
# Tier 2b — Firebase Authentication, where registered accounts are stored.
#
# All four are needed together; with any one missing, sign-in and registration
# fall back to the local password digest. Setting them up is documented in
# docs/FIREBASE.md.
#
# The password is checked on the server, not in the browser, so no Firebase SDK
# ships to the client and the session stays the same httpOnly cookie.
# ---------------------------------------------------------------------------

# Firebase console -> Project settings -> General -> Project ID.
FIREBASE_PROJECT_ID=

# From the service-account JSON: Project settings -> Service accounts ->
# Generate new private key. These two are SECRET — the JSON file must never be
# committed or shipped to the client.
FIREBASE_CLIENT_EMAIL=
# Paste the JSON's private_key value, quoted, with its newlines left as the two
# characters \n. The server converts them back.
#   FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"
FIREBASE_PRIVATE_KEY=

# Project settings -> General -> Your apps -> Web app -> apiKey. This one is a
# public identifier rather than a secret; it is here rather than under a VITE_
# name only because the password check runs server-side.
FIREBASE_API_KEY=

# ---------------------------------------------------------------------------
# Tier 3 — optional. Each degrades cleanly; nothing else breaks when unset.
# ---------------------------------------------------------------------------

# AI search refinement. Unset, the refine call returns PRECONDITION_FAILED and
# the storefront falls back to local catalog filtering, which still works.
MISTRAL_API_KEY=

# Umami analytics. Both are inlined into index.html, so both are public. Unset,
# the tag ships with its placeholders unsubstituted and fires one broken request
# per page load — the Pages workflow strips it for that reason.
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=

# ---------------------------------------------------------------------------
# Build-time flags (not secrets)
# ---------------------------------------------------------------------------

# Serve from a sub-path, e.g. /NetLet-web-app/ for a GitHub Pages project site.
# Unset means the domain root.
# VITE_BASE_PATH=

# Build the fixture-backed static demo with no backend: tRPC resolves from
# client/src/lib/demoLink.ts. Leave unset for a real deployment.
# VITE_DEMO_MODE=

# Server port. Defaults to 3000.
# PORT=
